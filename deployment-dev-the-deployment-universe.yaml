apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    note: Hello, I am dev!
  labels:
    app: hello
    variant: dev
  name: dev-the-deployment-universe
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hello
      deployment: hello-universe
      variant: dev
  template:
    metadata:
      annotations:
        note: Hello, I am dev!
      labels:
        app: hello
        deployment: hello-universe
        paramountcommerce.com/domain: platform
        test: "19"
        variant: dev
    spec:
      containers:
      - command:
        - /hello
        - --port=8080
        - --enableRiskyFeature=$(ENABLE_RISKY)
        env:
        - name: ALT_GREETING
          valueFrom:
            configMapKeyRef:
              key: altGreeting
              name: dev-the-map-universe
        - name: ENABLE_RISKY
          valueFrom:
            configMapKeyRef:
              key: enableRisky
              name: dev-the-map-universe
        image: monopole/hello:2
        name: the-container
        ports:
        - containerPort: 8080
