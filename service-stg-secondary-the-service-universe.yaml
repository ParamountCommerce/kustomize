apiVersion: v1
kind: Service
metadata:
  annotations:
    note: Hello, I am stg-secondary!
  labels:
    app: hello
    variant: stg-secondary
  name: stg-secondary-the-service-universe
spec:
  ports:
  - port: 8666
    protocol: TCP
    targetPort: 8080
  selector:
    app: hello
    deployment: hello-universe
    variant: stg-secondary
  type: LoadBalancer
